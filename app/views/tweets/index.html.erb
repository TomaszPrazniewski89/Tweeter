

<div class="clearifx">
<div class="row">

  <div class="col-md-3">
    <%= binding.pry %>
    <table>
      <tbody>


    <tr>
      <td>Tweets</td>
      <td>Following</td>
      <td>Followers</td>
    </tr>
    <tr>
      <td><%=Tweet.where("user_id =?", current_user.id).count %></td>
      <td>1222</td>
      <td>1555</td>
    </tr>
  </tbody>
</table>
  </div>


  <div class="col-md-3">

<h1>Tweets</h1>
<%= render 'form', tweet: @tweet %>

<table>
  <thead>
    <tr>
      <th>Tweet</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tweets.each do |tweet| %>
      <tr>
        <tr>
          <td>created by:<strong><%= current_user.name %></strong></td>

        </tr>
        <td><%= tweet.tweet %></td>
        <% if(current_user.id==tweet.user_id) %>
        <td><%= link_to 'Show', tweet %></td>
        <td><%= link_to 'Edit', edit_tweet_path(tweet) %></td>
        <td><%= link_to 'Destroy', tweet, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end%>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%# <%= link_to 'New Tweet', new_tweet_path %>
</div>




</div>
</div>
